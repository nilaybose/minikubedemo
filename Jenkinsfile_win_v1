pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                cmd './gradlew.bat build'
            }
        }
        stage('Docker') {
            steps {
            	script {
                	docker.build "nilaybose/mkubedemo:latest"
      				withDockerRegistry([ credentialsId: "gitdocker", url: "" ]) {
      					 cmd 'docker push nilaybose/mkubedemo:latest'
      				}
                }
           }
        }
        stage('Deploy V1') {
            steps {
                cmd 'kubectl create -f ./v1_deploy.yaml --record'
            }
        }
    }
}


